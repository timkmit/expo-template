import { Minus, Plus } from '@tamagui/lucide-icons'
import { Button, H3, Paragraph, XStack, YStack } from 'tamagui'

import useAppStore from '@/store/app'
import MainContainer from '@/navigation/navigation/MainContainer'

const Home = () => {
  return (
    
      <MainContainer/>
    
  )
}

export default Home
